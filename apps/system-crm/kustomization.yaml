# =============================================================================
# Kustomization para System CRM
# =============================================================================

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - secrets.yaml
  - configmap.yaml
  - pvc.yaml
  - postgres.yaml
  - deployment.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: system-crm
  app.kubernetes.io/managed-by: argocd
