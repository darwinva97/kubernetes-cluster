# =============================================================================
# Kustomization para WAPI
# =============================================================================

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - secrets.yaml
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: wapi
  app.kubernetes.io/managed-by: argocd
