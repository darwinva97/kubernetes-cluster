apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  # Secret se crea manualmente con el token real de Cloudflare
  # kubectl create secret generic cloudflare-api-token -n external-dns --from-literal=api-token=<TOKEN>
  # - secret.yaml
  - rbac.yaml
  - deployment.yaml
