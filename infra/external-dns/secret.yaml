# =============================================================================
# CLOUDFLARE API TOKEN SECRET
# =============================================================================
# IMPORTANTE: Este archivo es un placeholder.
# El secret real debe crearse manualmente o via Sealed Secrets:
#
# kubectl create secret generic cloudflare-api-token \
#   --namespace external-dns \
#   --from-literal=api-token=YOUR_CLOUDFLARE_API_TOKEN
#
# El token debe tener permisos:
# - Zone:Zone:Read
# - Zone:DNS:Edit
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-api-token
  namespace: external-dns
  labels:
    app.kubernetes.io/name: external-dns
  annotations:
    # Marcar como placeholder - ArgoCD no lo sobreescribirá si ya existe
    argocd.argoproj.io/compare-options: IgnoreExtraneous
type: Opaque
stringData:
  # PLACEHOLDER - Reemplazar con token real via kubectl
  api-token: "REPLACE_ME_WITH_REAL_TOKEN"
